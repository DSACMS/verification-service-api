extends:
  - spectral:oas
rules:
  operation-summary-required:
    description: Every operation must have a summary.
    given: "$.paths[*][*]"
    severity: warn
    then:
      field: summary
      function: truthy
