# Documentation Accuracy Review (as of March 23, 2026)

## Section A: Executive Summary

**Report date:** `2026-03-23`  
**Scope:** `root docs + docs/**/*.md`  
**Overall accuracy score:** **41/100**  
**Completion gate status:** **FAIL**

**Risk statement:**
- The public EDU contract described in planning docs and OpenAPI artifacts does not match the current Fiber runtime, which still serves a scaffolded `GET /api/edu` flow.
- Authentication, consent-header, and error-envelope documentation overstate behavior that the runtime does not currently enforce.
- Contributor-facing root docs still contain broken placeholders and incomplete setup/ownership guidance, which raises onboarding and maintenance risk.

## Section B: Severity-Ranked Findings

| Severity | Doc location | Observed mismatch | Source-of-truth evidence on current branch | Recommended fix |
|---|---|---|---|---|
| `P1` | `docs/planning/public-api-data-model-design-first.md:67`; `docs/features/core/edu-openapi-spec.md:5`; `api-spec/openapi.yaml:19`; `api-spec/paths/edu.yaml:1` | The docs/spec present a merged public contract centered on `POST /v1/edu`, but the runtime only registers `GET /api/edu` and the handler still builds a hard-coded vendor request instead of accepting the documented request body. | `api/routes/router.go:24`; `api/routes/router.go:40`; `api/handlers/education_handler.go:48`; `api/handlers/education_handler_test.go:37`; `api/handlers/education_handler_test.go:55` | Either implement the documented versioned `POST /v1/edu` contract end-to-end or clearly relabel the spec/docs as proposed and document the current scaffolded `GET /api/edu` behavior. |
| `P1` | `docs/features/core/edu-openapi-spec.md:15`; `api-spec/paths/edu.yaml:10` | The contract docs require `BearerAuth`, `X-EMMY-Consent-Token`, and RFC 7807 problem responses, but the runtime validates `x-amzn-oidc-accesstoken`, never checks the consent header, and emits plain-text Fiber error bodies. | `api/middleware/middleware.go:100`; `api/middleware/middleware.go:129`; `api/app.go:24`; `api/app.go:37`; `api/handlers/education_handler.go:74` | Align the runtime to the documented auth/consent/error contract, or revise the docs/spec so they explicitly describe the current Cognito-header and plain-text error behavior. |
| `P2` | `README.md:41` | The SBOM section still contains a cookiecutter placeholder URL (`https://github.com/{{ ... }}`), so the advertised dependency graph link is not actionable. | `go.mod:1` | Replace the placeholder with the real repository dependency-graph URL or remove the sentence until a valid SBOM/dependency page is available. |
| `P2` | `CONTRIBUTING.md:1`; `COMMUNITY.md:7` | Contributor and ownership docs remain template placeholders even though the repository already contains concrete setup artifacts and automation. | `.env.example:1`; `.pre-commit-config.yaml:1`; `docker-compose.yml:1`; `go.mod:1` | Fill in actual build/run/test/setup instructions, issue guidance, and named project contacts instead of leaving TODO/template content in place. |
| `P2` | `docs/features/README.md:3` | The feature index says the directory is organized for quick navigation, but it omits the existing core feature doc `docs/features/core/edu-openapi-spec.md`. | `docs/features/core/edu-openapi-spec.md:1` | Add `edu-openapi-spec.md` to the Core table or narrow the README description so it does not imply a complete index. |

No standalone `P3` findings were recorded once higher-severity inaccuracies were accounted for.

## Section C: Update Backlog Checklist by Doc File

### `docs/planning/public-api-data-model-design-first.md`
- [ ] Mark the design-first contract as proposed-only until runtime matches it, or update the document to describe the current scaffold/runtime gap explicitly.

### `docs/features/core/edu-openapi-spec.md`
- [ ] Replace statements that imply the `POST /v1/edu` contract is already live, or finish the implementation and keep the feature doc aligned with it.
- [ ] Correct the documented auth, consent-header, and error-envelope behavior to match the live server behavior.

### `api-spec/openapi.yaml`
- [ ] Decide whether this file describes a live contract or a proposed future contract, then reflect that choice in route shape, auth scheme, and versioning references.

### `api-spec/paths/edu.yaml`
- [ ] Remove or revise undocumented runtime requirements (`POST`, consent token, RFC 7807 responses) unless the server is updated to enforce them.

### `README.md`
- [ ] Replace the placeholder SBOM link with a real dependency/SBOM destination.

### `CONTRIBUTING.md`
- [ ] Add concrete local setup, build, test, and issue-reporting instructions based on the existing Go, Docker, env, and pre-commit files.

### `COMMUNITY.md`
- [ ] Replace placeholder role/name cells and TODO comments with actual project contacts and affiliations.

### `docs/features/README.md`
- [ ] Add `core/edu-openapi-spec.md` to the index or explicitly scope the index to only a subset of feature docs.

## Section D: Hygiene Appendix

### Commands run
- `rg --files -g '*.md' .`
- `git ls-files`
- `sed -n '1,260p' README.md`
- `sed -n '1,260p' CONTRIBUTING.md`
- `sed -n '1,220p' COMMUNITY.md`
- `sed -n '1,220p' SECURITY.md`
- `sed -n '1,260p' main.go`
- `sed -n '1,260p' api/app.go`
- `sed -n '1,260p' api/routes/router.go`
- `sed -n '1,260p' api/handlers/education_handler.go`
- `sed -n '1,320p' api/middleware/middleware.go`
- `sed -n '1,260p' api-spec/openapi.yaml`
- `sed -n '1,260p' api-spec/paths/edu.yaml`
- `rg -n --hidden --no-ignore '(BEGIN (RSA|EC|OPENSSH|DSA) PRIVATE KEY|AKIA[0-9A-Z]{16}|ASIA[0-9A-Z]{16}|ghp_[A-Za-z0-9]{36}|xox[baprs]-[A-Za-z0-9-]+|aws_secret_access_key|AIza[0-9A-Za-z_-]{35})' $(git ls-files)`
- `rg -n '\[[^]]+\]\(([^)]+)\)' README.md CONTRIBUTING.md COMMUNITY.md SECURITY.md docs`
- `for p in COMMUNITY.md SECURITY.md LICENSE docs/features/core/nsc-education.md docs/features/infrastructure/redis.md docs/features/infrastructure/opentelemetry.md docs/features/infrastructure/prometheus.md docs/features/infrastructure/jaeger.md docs/features/security/cognito-auth.md docs/features/resilience/circuit-breaker.md; do test -e "$p" && echo "OK $p" || echo "MISSING $p"; done`

### Sensitive-file scan findings
- No matches were returned for common private-key and token patterns across tracked files.

### Markdown link/reference/path validation findings
- Reviewed repo-local markdown targets resolve correctly for `COMMUNITY.md`, `SECURITY.md`, `LICENSE`, and the indexed `docs/features/*` pages that are linked from audited docs.
- External URLs were not dereferenced during this audit.
- `README.md:41` contains a malformed placeholder link and is already captured as a `P2` finding.

### Hygiene verdict
- **Blocker:** The documentation set cannot receive a `PASS` while the EDU contract mismatch (`P1`) and contributor-facing placeholder/broken-link issues (`P2`) remain unresolved.

## Section E: Deferred Watchlist (Non-blocking)

- If the EDU runtime moves from the current scaffold to a request-body-driven public contract, update:
  - `docs/features/core/nsc-education.md`
  - `docs/features/core/edu-openapi-spec.md`
  - `docs/planning/public-api-data-model-design-first.md`
- If the authentication model changes from the current Cognito access-token header flow to bearer tokens and explicit consent enforcement, update:
  - `docs/features/security/cognito-auth.md`
  - `api-spec/openapi.yaml`
  - `api-spec/paths/edu.yaml`
- If additional feature docs continue to be added under `docs/features/`, update:
  - `docs/features/README.md`
