# Documentation Accuracy Review (as of March 23, 2026)

## Section A: Executive Summary

**Report date:** `2026-03-23_15:30:57`  
**Scope:** `root docs + docs/**/*.md`  
**Overall accuracy score:** **47/100**  
**Completion gate status:** **FAIL**

**Risk statement:**
- The primary EDU contract docs still describe a live public API shape that the current Fiber runtime does not implement.
- The newly added setup and architecture docs improved coverage, but some of their caveats now contradict the current code and can mislead contributors.
- Root and navigation docs still contain unresolved placeholders or broken paths, which keeps onboarding and doc discoverability below pass level.

## Section B: Severity-Ranked Findings

| Severity | Doc location | Observed mismatch | Source-of-truth evidence on current branch | Recommended fix |
|---|---|---|---|---|
| `P1` | `docs/planning/public-api-data-model-design-first.md:67`; `docs/features/core/edu-openapi-spec.md:5`; `docs/features/core/nsc-education.md:4`; `api-spec/openapi.yaml:19`; `api-spec/paths/edu.yaml:1` | The documentation set presents a body-driven `POST /v1/edu` public contract, but the current runtime only exposes `GET /api/edu` and the handler still builds the NSC payload internally instead of accepting the documented request body. | `api/routes/router.go:24`; `api/routes/router.go:40`; `api/handlers/education_handler.go:48`; `api/handlers/education_handler.go:84` | Either implement the documented `POST /v1/edu` contract end-to-end or clearly relabel the spec/docs as proposed and document the current scaffolded `GET /api/edu` behavior instead. |
| `P1` | `docs/features/core/edu-openapi-spec.md:15`; `api-spec/paths/edu.yaml:9` | The contract docs require bearer auth, `X-EMMY-Consent-Token`, and RFC 7807 problem responses, but the runtime validates `x-amzn-oidc-accesstoken`, never checks the consent header, and returns plain-text Fiber error bodies. | `api/middleware/middleware.go:100`; `api/middleware/middleware.go:129`; `api/app.go:37`; `api/handlers/education_handler.go:74` | Align runtime auth, consent, and error behavior to the published contract, or revise the docs/spec so they describe the actual Cognito-header and plain-text error semantics. |
| `P2` | `docs/setup.md:19`; `docs/architecture.md:90` | The new docs say the listener is still hardcoded to `:8000`, `SKIP_AUTH` does not work, and `.env.example` uses mixed-case keys, but the current code listens on the parsed port, applies `SkipAuthMiddleware`, and `.env.example` already uses uppercase `PORT`/`ENVIRONMENT`. | `main.go:112`; `main.go:141`; `api/app.go:106`; `api/app.go:117`; `.env.example:1` | Remove the stale fixme bullets and keep only caveats that still reflect the current branch. |
| `P2` | `README.md:41` | The SBOM section still contains a cookiecutter placeholder URL, so the advertised dependency graph/SBOM destination is not actionable. | `go.mod:1` | Replace the placeholder with the real repository dependency-graph or SBOM URL, or remove the sentence until one exists. |
| `P2` | `CONTRIBUTING.md:1`; `COMMUNITY.md:7` | Contributor and ownership docs remain template placeholders despite the repository now having concrete setup and architecture docs that should be linked and summarized. | `docs/setup.md:1`; `docs/overview.md:1`; `go.mod:1`; `.pre-commit-config.yaml:1` | Replace TODO/template content with real contribution workflow, setup pointers, and named project contacts. |
| `P3` | `docs/features/README.md:3` | The feature index claims to organize the feature docs for navigation, but it still omits `docs/features/core/edu-openapi-spec.md` from the Core table. | `docs/features/core/edu-openapi-spec.md:1` | Add the missing Core entry or narrow the README text so it no longer implies a complete feature index. |

## Section C: Update Backlog Checklist by Doc File

### `docs/planning/public-api-data-model-design-first.md`
- [ ] Mark the OpenAPI contract as proposed until runtime matches it, or update the document to describe the currently implemented `/api/edu` scaffold explicitly.

### `docs/features/core/edu-openapi-spec.md`
- [ ] Remove statements that imply `POST /v1/edu` is already live unless the route, request binding, auth, consent, and error envelope are all implemented.

### `docs/features/core/nsc-education.md`
- [ ] Clarify that the current HTTP handler does not yet accept caller-provided JSON input and is still scaffolded around an internally built request body.

### `api-spec/openapi.yaml`
- [ ] Decide whether this file is documenting a live contract or a design target, then make its path/versioning/auth assumptions match that choice.

### `api-spec/paths/edu.yaml`
- [ ] Remove or revise the undocumented runtime requirements (`POST`, consent token, bearer auth, RFC 7807 responses) unless the server is updated to enforce them.

### `docs/setup.md`
- [ ] Delete the stale caveat bullets about hardcoded port, broken `SKIP_AUTH`, and mixed-case `.env.example` keys.
- [ ] Keep only the remaining real gaps, such as missing env examples and Redis/docker workflow caveats.

### `docs/architecture.md`
- [ ] Refresh the technical caveats list so it only includes still-true implementation gaps.

### `README.md`
- [ ] Replace the placeholder SBOM link with a real repository URL.

### `CONTRIBUTING.md`
- [ ] Replace template sections with actual contribution, setup, test, and issue-reporting guidance, and link to `docs/setup.md` where helpful.

### `COMMUNITY.md`
- [ ] Replace placeholder table rows and TODO comments with real project contacts and affiliations.

### `docs/features/README.md`
- [ ] Add `core/edu-openapi-spec.md` to the Core table so the feature index is complete.

## Section D: Hygiene Appendix

### Commands run
- `date '+%Y-%m-%d_%H:%M:%S'`
- `rg --files -g '*.md' . | sort`
- `git ls-files '*.md' | sort`
- `nl -ba docs/overview.md | sed -n '1,260p'`
- `nl -ba docs/setup.md | sed -n '1,320p'`
- `nl -ba docs/api.md | sed -n '1,320p'`
- `nl -ba docs/architecture.md | sed -n '1,320p'`
- `nl -ba docs/research/auth-token-validation-strategy.md | sed -n '1,260p'`
- `nl -ba docs/research/circuit-breaker-state-management.md | sed -n '1,260p'`
- `nl -ba docs/research/concurrency-model.md | sed -n '1,260p'`
- `nl -ba docs/research/observability-stack.md | sed -n '1,260p'`
- `nl -ba docs/features/README.md | sed -n '1,220p'`
- `nl -ba docs/features/core/edu-openapi-spec.md | sed -n '1,220p'`
- `nl -ba docs/features/core/nsc-education.md | sed -n '1,120p'`
- `nl -ba main.go | sed -n '1,220p'`
- `nl -ba api/app.go | sed -n '1,220p'`
- `nl -ba api/routes/router.go | sed -n '1,220p'`
- `nl -ba api/routes/status_router.go | sed -n '1,200p'`
- `nl -ba api/handlers/education_handler.go | sed -n '1,260p'`
- `nl -ba api/handlers/status_handler.go | sed -n '1,200p'`
- `nl -ba api/middleware/middleware.go | sed -n '90,220p'`
- `nl -ba api-spec/openapi.yaml | sed -n '1,200p'`
- `nl -ba api-spec/paths/edu.yaml | sed -n '1,220p'`
- `nl -ba .env.example | sed -n '1,220p'`
- `rg -n --hidden --no-ignore '(BEGIN (RSA|EC|OPENSSH|DSA) PRIVATE KEY|AKIA[0-9A-Z]{16}|ASIA[0-9A-Z]{16}|ghp_[A-Za-z0-9]{36}|xox[baprs]-[A-Za-z0-9-]+|aws_secret_access_key|AIza[0-9A-Za-z_-]{35})' $(git ls-files)`
- `rg -n '\[[^]]+\]\(([^)]+)\)' README.md CONTRIBUTING.md COMMUNITY.md SECURITY.md docs`
- `for p in docs/architecture.md docs/setup.md docs/api.md docs/features docs/research docs/planning docs/questions COMMUNITY.md SECURITY.md LICENSE docs/features/core/nsc-education.md docs/features/infrastructure/redis.md docs/features/infrastructure/opentelemetry.md docs/features/infrastructure/prometheus.md docs/features/infrastructure/jaeger.md docs/features/security/cognito-auth.md docs/features/resilience/circuit-breaker.md; do test -e "$p" && echo "OK $p" || echo "MISSING $p"; done`

### Sensitive-file scan findings
- No matches were returned for common private-key and token patterns across tracked files.

### Markdown link/reference/path validation findings
- The repo-local links reviewed in `README.md`, `CONTRIBUTING.md`, `SECURITY.md`, and `docs/features/README.md` resolve to existing targets.
- `docs/overview.md:68` links to `questions/`, but the repo-local path validation found `docs/questions` missing.
- `README.md:41` still contains a malformed placeholder link in the SBOM section.
- External URLs were not dereferenced during this audit.

### Hygiene verdict
- **Blocker:** Unresolved `P1` contract drift plus `P2` placeholder/broken-link issues prevent a `PASS`, and the overview navigation still contains a missing local path.

## Section E: Deferred Watchlist (Non-blocking)

- If the live EDU endpoint is upgraded from the current scaffold to a body-bound public API, update:
  - `docs/api.md`
  - `docs/overview.md`
  - `docs/features/core/nsc-education.md`
  - `docs/features/core/edu-openapi-spec.md`
  - `docs/planning/public-api-data-model-design-first.md`
- If `/status` wiring or Redis dependency semantics change, update:
  - `docs/api.md`
  - `docs/architecture.md`
  - `docs/overview.md`
  - `docs/setup.md`
- If a `docs/questions/` section is added later or overview navigation is simplified, update:
  - `docs/overview.md`
